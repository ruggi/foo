import * as React from 'react'
import { Storyboard, RemixScene } from 'utopia-api'

// hey there :D

export var storyboard = (
  <Storyboard>
    <RemixScene
      style={{
        width: 100,
        height: 100,
      }}
    />
  </Storyboard>
)
